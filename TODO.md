# TODO

- [x] Add Vercel static deployment configuration by ensuring Vercel is set to “Static” and uses repo root as output.
- [x] If Vercel still does not serve index.html, add a minimal `vercel.json` to force static files.
- [ ] Commit changes (if any) and push to GitHub.
- [ ] Trigger new Vercel deployment and verify `/` loads `index.html`.



